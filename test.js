// var initDb = require('./config/db_config');
// var mysql = require('mysql');

// var con = initDb(mysql);

// con.connect(function (err) {
//     if (err) throw err;
//     console.log('Connected to the database');
// });

console.log(Date.now());